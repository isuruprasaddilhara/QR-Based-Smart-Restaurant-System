import styles from "./Logo.module.css";

function Logo() {
  return (
    <div>
      <img src="Scan2Serve.png" alt="Scan2Serve Logo" className={styles.logo} />
    </div>
  );
}

export default Logo;
